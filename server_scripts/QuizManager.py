

class QuizManager:
    
        
    async def startQuiz(self, sid, data):
        await pass
        
    async def quizAttemptAnswer(self, sid, data):
        await pass
        
    async def quizAttemptAnswer(self, sid, data):
        await pass

    async def quizSubmitQuestion(self, sid, data):
        await pass
